<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Morgan Portfolio</title>

  <!-- CSS INSIDE HTML -->
  <style>
    /* RESET */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --primary: #2563eb;
      --secondary: #1e293b;
      --light: #f8fafc;
      --dark: #0f172a;
      --text: #1f2937;
      --muted: #6b7280;

      --font-base: 1rem;
      --font-lg: 1.25rem;
      --font-xl: 2rem;
      --font-xxl: 3rem;
    }

    body {
      font-family: Arial, sans-serif;
      line-height: 1.7;
      color: var(--text);
      background: var(--light);
    }

    /* NAVBAR */
    .navbar {
      display: flex;
      justify-content: space-between;
      padding: 1.2rem 2rem;
      background: var(--dark);
      color: white;
    }

    .nav-links a {
      margin-left: 1.2rem;
      text-decoration: none;
      color: white;
    }

    .logo {
      font-size: var(--font-lg);
    }

    /* HERO */
    .hero {
      height: 80vh;
      background: url("https://picsum.photos/1600/900") center/cover no-repeat;
      display: flex;
      align-items: center;
      padding-left: 5%;
    }

    .hero-text h1 {
      font-size: var(--font-xxl);
      color: white;
    }

    .hero-text span {
      color: var(--primary);
    }

    .hero-text p {
      margin: 1rem 0;
      color: white;
      font-size: var(--font-lg);
    }

    /* BUTTONS */
    .btn-primary, .btn-secondary {
      padding: 0.7rem 1.4rem;
      border-radius: 5px;
      text-decoration: none;
      font-weight: bold;
      display: inline-block;
      transition: .3s;
    }
    .btn-primary { background: var(--primary); color: white; }
    .btn-primary:hover { background: #1d4ed8; }
    .btn-secondary { background: var(--secondary); color: white; }
    .btn-secondary:hover { background: #334155; }

    /* SECTIONS */
    .section-title {
      text-align: center;
      margin: 3rem 0 1rem;
      font-size: var(--font-xl);
    }

    /* ABOUT */
    .about {
      width: 70%;
      margin: auto;
      text-align: center;
    }

    /* PROJECTS GRID */
    .projects-grid {
      width: 90%;
      margin: auto;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    .card {
      background: white;
      padding: 1rem;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      text-align: center;
    }

    .card img {
      width: 100%;
      border-radius: 10px;
      margin-bottom: .5rem;
    }

    /* CONTACT */
    .contact-form {
      width: 70%;
      margin: auto;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .contact-form input,
    .contact-form textarea {
      padding: 1rem;
      border: 1px solid #ddd;
      border-radius: 8px;
    }

    /* FOOTER */
    .footer {
      text-align: center;
      padding: 1.5rem;
      background: var(--dark);
      color: white;
      margin-top: 3rem;
    }
  </style>

</head>
<body>

  <!-- NAVBAR -->
  <header class="navbar">
    <h2 class="logo">Morgan</h2>
    <nav class="nav-links">
      <a href="#">Home</a>
      <a href="#about">About</a>
      <a href="#projects">Projects</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-text">
      <h1>Hello, I'm <span>Morgan</span></h1>
      <p>A Passionate Developer Learning HTML & CSS</p>
      <a href="#projects" class="btn-primary">View My Work</a>
    </div>
  </section>

  <!-- ABOUT -->
  <section id="about" class="about">
    <h2 class="section-title">About Me</h2>
    <p>
      I am a beginner developer from Nairobi learning programming.
      This portfolio is part of my CSS Mastery Task (Week 2).
    </p>
  </section>

  <!-- PROJECTS -->
  <section id="projects" class="projects">
    <h2 class="section-title">Projects</h2>

    <div class="projects-grid">
      <div class="card">
        <img src="https://picsum.photos/400/250" alt="">
        <h3>Project One</h3>
        <p>A card layout built using CSS.</p>
      </div>

      <div class="card">
        <img src="https://picsum.photos/400/250?2" alt="">
        <h3>Project Two</h3>
        <p>A responsive grid layout.</p>
      </div>

      <div class="card">
        <img src="https://picsum.photos/400/250?3" alt="">
        <h3>Project Three</h3>
        <p>A flexbox-based layout project.</p>
      </div>
    </div>
  </section>

  <!-- CONTACT -->
  <section id="contact" class="contact">
    <h2 class="section-title">Contact Me</h2>

    <form class="contact-form">
      <input type="text" placeholder="Your Name">
      <input type="email" placeholder="Email Address">
      <textarea rows="5" placeholder="Message"></textarea>
      <button class="btn-primary">Send Message</button>
    </form>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <p>© 2026 Morgan • All Rights Reserved</p>
  </footer>

</body>
</html>